# PR 状态 ↔ mr-board 看板状态梳理与对齐方案

## 一、Git 平台原生状态（platformStatus）

| 平台 | 原生状态 | 说明 |
|------|---------|------|
| GitHub | `open` | PR 打开中 |
| GitHub | `closed` | PR 已关闭（未合并） |
| GitHub | `merged` | PR 已合并 |
| GitHub | `draft` | 草稿 PR（通过 `draft=true` 字段或 `title` 前缀） |
| GitLab | `opened` / `open` | MR 打开中 |
| GitLab | `closed` | MR 已关闭 |
| GitLab | `merged` | MR 已合并 |

## 二、mr-board 看板状态（boardStatus）— 7 列

| 状态码 | 中文标签 | 颜色 | 业务含义 |
|--------|---------|------|---------|
| `pending_review` | 待 Review | #909399 | 无 reviewer / review 未开始 |
| `reviewing` | Review 中 | #e6a23c | 有 reviewer，但未 approved（或 changes requested） |
| `ci_checking` | CI 检查中 | #409eff | CI 正在运行或等待中 |
| `conflict` | 冲突待解决 | #f56c6c | **当前语义过载**：代码冲突、CI 失败、approved 但不可合并 |
| `ready` | 可合并 | #67c23a | CI 通过 + Review 通过 + 无冲突 + mergeable |
| `merged` | 已合并 | #409eff | 已合并（终态） |
| `closed` | 已关闭 | #909399 | 已关闭（终态） |

## 三、当前映射逻辑（BoardStatusCalculator）

```
platformStatus
├── merged ──────────────────────────────→ merged
├── closed ──────────────────────────────→ closed
└── open
    ├── title = "draft:*" / "wip:*" ─────→ pending_review
    ├── hasConflict = true ──────────────→ conflict（代码冲突）
    ├── ciStatus = running/pending ──────→ ci_checking
    ├── ciStatus = failed ───────────────→ conflict（CI 失败）
    └── 以上均无
        ├── 无 reviewer / approval=pending → pending_review
        ├── approval = changes_requested ─→ reviewing
        ├── approval = reviewing ─────────→ reviewing
        ├── approval = approved
        │   ├── mergeable = true ─────────→ ready
        │   └── mergeable = false ────────→ conflict（非冲突原因）
        └── 其他 ─────────────────────────→ pending_review
```

## 四、当前问题诊断

### 4.1 `conflict` 列语义过载（最严重）
当前 3 种完全不同的阻塞原因都落入同一列：

| 实际原因 | 进入条件 | 用户误解 |
|---------|---------|---------|
| **代码冲突** | `hasConflict=true` | 正确理解为冲突 |
| **CI 失败** | `ciStatus=failed` | 看到红框以为是冲突，实则是构建失败 |
| **approved 但不可合并** | `approval=approved && mergeable=false` | 看到红框以为是冲突，实则是分支保护/需变基 |

### 4.2 `ci_checking` 列内缺乏区分
- `ciStatus=running`（正在跑 CI）和 `ciStatus=pending`（等待中）都显示为 `ci_checking`
- 用户无法一眼区分"正在跑"和"还没开始"

### 4.3 Draft 检测不精确
- 仅通过 `title` 前缀 `draft:` / `wip:` 检测
- GitHub 有原生 `draft` 字段未使用

## 五、对齐方案（推荐）

### 方案 A：增强列内语义标签（最小改动，推荐）

保持 7 列不变，在 `conflict` 和 `ci_checking` 列的卡片上增加**次级原因标签**，让用户一眼知道具体阻塞原因。

#### 5.1 `conflict` 列卡片增强

```vue
<!-- MrCard.vue -->
<el-tag v-if="status === 'conflict'" size="small" :type="conflictReasonType">
  {{ conflictReasonLabel }}
</el-tag>
```

| 实际原因 | 判断条件 | 标签文案 | 标签颜色 |
|---------|---------|---------|---------|
| 代码冲突 | `hasConflict=true` | `代码冲突` | danger |
| CI 失败 | `ciStatus=failed` | `CI 失败` | danger |
| 不可合并 | `approval=approved && mergeable=false` | `需变基/保护规则` | warning |

#### 5.2 `ci_checking` 列卡片增强

在卡片 `CiStatusIcon` 旁边增加文字说明：

| ciStatus | 图标 | 文案 |
|---------|------|------|
| `running` | 橙色旋转 Loading | `CI 运行中` |
| `pending` | 灰色 Minus | `CI 等待中` |

#### 5.3 BoardStatusCalculator 映射微调

```java
// 当前逻辑已合理，只需保证 ciFailed 进入 conflict 时携带 ciStatus 信息
// 无需修改状态码，因为状态码只是列归属，真正原因由 ciStatus / hasConflict / mergeable 字段表达
```

### 方案 B：拆分 `conflict` 为两列（较大改动）

如果产品明确要求 CI 失败和代码冲突分开展示：

| 新状态码 | 中文标签 | 来源 |
|---------|---------|------|
| `conflict` | 代码冲突 | `hasConflict=true` |
| `ci_failed` | CI 失败 | `ciStatus=failed` |

**缺点**：
- 看板从 7 列 → 8 列，空间更紧张
- 需要修改前后端常量、拖拽规则、报表统计等大量文件
- `approved 但不可合并` 仍然需要归属到某一列

**结论**：不建议。

### 方案 C：状态矩阵重构（远期）

将"阻塞原因"从状态中剥离，看板状态只保留：
- `pending_review` / `reviewing` / `ready` / `merged` / `closed`
- CI 状态作为独立维度图标展示
- 冲突/失败作为卡片上的独立标签展示

**缺点**：改动量极大，涉及前后端所有状态流转逻辑。

## 六、实施建议（按方案 A）

### 6.1 后端（无需改动）
`BoardStatusCalculator` 的映射逻辑保持不变，因为：
- `conflict` 列作为"阻塞合并区"是合理的业务抽象
- 具体阻塞原因已由 `hasConflict`、`ciStatus`、`mergeable` 字段携带

### 6.2 前端改动点

| 文件 | 改动内容 |
|------|---------|
| `MrCard.vue` | 增加 `conflictReason` 计算属性，当 `status==='conflict'` 时在 card-header 或 title 下方显示原因标签 |
| `MrCard.vue` | `ci_checking` 列卡片在 `CiStatusIcon` 旁增加文字状态说明 |
| `DashboardView.vue` | `ci_checking` 列的列标题可增加 CI 图标，强化视觉认知 |

### 6.3 数据迁移
无需迁移，当前数据已满足要求。

## 七、状态流转速查表

| 平台状态 | CI 状态 | Review 状态 | hasConflict | mergeable | 看板状态 | 原因标签 |
|---------|---------|------------|-------------|-----------|---------|---------|
| open | running | pending | false | true | ci_checking | CI 运行中 |
| open | failed | approved | false | true | conflict | CI 失败 |
| open | success | approved | false | true | ready | — |
| open | success | approved | false | false | conflict | 需变基/保护规则 |
| open | success | approved | true | false | conflict | 代码冲突 |
| open | success | reviewing | false | true | reviewing | — |
| open | success | pending | false | true | pending_review | — |
| merged | — | — | — | — | merged | — |
| closed | — | — | — | — | closed | — |

## 八、结论

**当前 7 列设计是合理的**，问题不在状态划分，而在 `conflict` 列的**语义表达不足**。

推荐采用**方案 A**：
1. 保持 `BoardStatusCalculator` 不变
2. 在 `MrCard.vue` 中为 `conflict` 列卡片增加**原因标签**（代码冲突 / CI 失败 / 需变基）
3. 在 `ci_checking` 列卡片中明确区分 `running` 和 `pending`

这样可以在不改动状态体系的前提下，解决评审提出的"列内 CI 区分"和"approved 但不可合并的 tooltip"问题。
